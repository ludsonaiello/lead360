NODE_ENV=development
PORT=7000
HOSTNAME=127.0.0.1

# API base URL (server-side)
API_BASE_URL=https://api.lead360.app

# API base URL (client-side - for Next.js frontend)
NEXT_PUBLIC_API_URL=https://api.lead360.app/api/v1

#BASE APP URL
NEXT_PUBLIC_APP_URL=https://app.lead360.app

# Cookie domain for tenants later (keep commented until auth is built)
# COOKIE_DOMAIN=.lead360.app

#GOOGLE MAPS API (NEXT_PUBLIC_ prefix required for client-side access)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Upload directory (NEXT_PUBLIC_ prefix required for client-side access)
NEXT_PUBLIC_UPLOAD_DIR=https://app.lead360.app/uploads/public
UPLOAD_DIR=https://app.lead360.app/uploads/public

USER_ADMIN=
PWD_ADMIN=

USER_TENANT=
PWD_TENANT=

GMAIL_App_Password_ludsonaiello=
BREVO_API=

AMAZONN_IAM=
AMAZON_USR_SMTP=
AMAZON_KEY_SMTP=
AWS_API_KEY=
AWS_SECRET_KEY=
# Webhook configuration (for Twilio webhooks)
NEXT_PUBLIC_APP_DOMAIN=lead360.app
NEXT_PUBLIC_WEBHOOK_PROTOCOL=https

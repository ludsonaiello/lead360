'use client';

import React from 'react';
import VisualBuilder from '@/components/template-builder/VisualBuilder';

/**
 * Create New Visual Template Page
 * Allows platform admins to create new visual templates using drag-and-drop builder
 */
export default function NewVisualTemplatePage() {
  return <VisualBuilder />;
}
